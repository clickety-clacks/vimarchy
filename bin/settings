#!/usr/bin/env bash

set -euo pipefail

plugin_dir=$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)

# Leave the selection mode before replacing it with the keyboard-passive
# settings surface.
"$plugin_dir/bin/cancel"
omarchy-shell shell summon vimarchy '{"mode":"settings"}' >/dev/null
